{
  "name": "cow-token-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "prepare:mainnet": "mustache config/mainnet.json subgraph.yaml.mustache > subgraph.yaml",
    "prepare:gchain": "mustache config/gchain.json subgraph.yaml.mustache > subgraph.yaml",
    "codegen": "graph codegen",
    "build": "graph codegen && graph build",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.28.0",
    "@graphprotocol/graph-ts": "0.26.0"
  },
  "devDependencies": {
    "matchstick-as": "^0.4.0",
    "mustache": "^4.2.0"
  }
}
